#include "stdafx.h"
#include "GzColor.h"


GzColor::GzColor()
	: Red(0), Green(0), Blue(0), Alpha(0)
{
}

GzColor::GzColor(float r, float g, float b, float a)
	: Red(r), Green(g), Blue(b), Alpha(a)
{
}